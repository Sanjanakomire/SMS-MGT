import React from 'react';
import './Dashboard.css';

const Dashboard = () => {
  // Fetch and display metrics here
return (
    <div className="dashboard-container">
    <h1>Dashboard</h1>
    <p>Metrics will be displayed here.</p>
    </div>
);
};

export default Dashboard;
